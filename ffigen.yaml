# Run with `dart run ffigen --config ffigen.yaml`.
name: ZvecFlutterAndroidBindings
description: |
  Bindings for `src/zvec_flutter_android.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/zvec_flutter_android_bindings_generated.dart'
headers:
  entry-points:
    - 'src/zvec_flutter_android.h'
  include-directives:
    - 'src/zvec_flutter_android.h'
comments:
  style: any
  length: full
