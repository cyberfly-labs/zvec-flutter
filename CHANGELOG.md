## 0.0.1

* Initial Android-only Flutter FFI wrapper for bundled zvec.
* Adds a Python-like Dart API with collection, schema, doc, and query helpers.
* Includes an Android example app demonstrating local indexing and vector search.
* Publishes prebuilt Android JNI libraries by default to reduce package size.
